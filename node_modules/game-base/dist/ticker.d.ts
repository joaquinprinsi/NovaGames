export type Phase = "head" | "update" | "render" | "tail";
export type Listener = (delta: number) => void;
declare class Ticker {
    #private;
    phases: Readonly<{
        head: Set<Listener>;
        update: Set<Listener>;
        render: Set<Listener>;
        tail: Set<Listener>;
    }>;
    get running(): boolean;
    private tick;
    register(listener: Listener, phase?: Phase): void;
    unregister(listener: Listener, phase?: Phase): void;
    start: () => void;
    stop: () => void;
}
export default Ticker;
